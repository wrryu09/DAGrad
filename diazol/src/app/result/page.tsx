import TopBar from "@/components/common/TopBar";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <TopBar />
    </div>
  );
};

export default page;
